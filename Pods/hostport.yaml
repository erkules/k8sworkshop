apiVersion: v1
kind: Pod
metadata:
  name: hostport
spec:
  containers:
  - name: ping
    image: alpine
    command:
    - sleep
    - "5000"
    ports:
    - containerPort: 80
      hostPort: 60111

