apiVersion: v1
kind: Pod
metadata:
  name: debian-32bit
spec:
#  affinity:
#    nodeAffinity:
#      requiredDuringSchedulingIgnoredDuringExecution:
#        nodeSelectorTerms:
#        - matchExpressions:
#          - key: kubernetes.io/arch
#            operator: In
#            values:
#            - amd64 # Läuft auf 64-Bit Host...
  containers:
  - name: debian
    image: i386/debian:bookworm-slim # Explizites 32-Bit Image nutzen
    command:
    - sleep
    - "5000"
